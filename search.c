#include <stdio.h>
int main(void)
{
    // int numbers[] = {9, 8, 4, 78, 32, 1};
    int n, s;
    printf("Enter a number: ");
    scanf("%d", &n);
    s = n * n;
    printf("squared number is %d\n", s);
}